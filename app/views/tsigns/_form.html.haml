<%= form_for(@tsign) do |f| %>
  <% if @tsign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tsign.errors.count, "error") %> prohibited this tsign from being saved:</h2>

      <ul>
      <% @tsign.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :signature %><br>
    <%= f.text_area :signature %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
