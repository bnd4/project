= form_for @tsign do |f|
  =error_messages_for @tsign

  .form-group
    = f.label :signature
    = f.text_field :signature,class: 'form-control'

  .form-actions
    = f.submit nil, class: 'btn btn-primary'