%h2
  Шапки

%p.n1
  %b Содержание шапки:
  = @thead.head

= link_to 'Edit', edit_thead_path(@thead), class: 'btn btn-primary'
= link_to 'Back', theads_path, class: 'btn btn-primary'
= link_to @thead, class: 'btn btn-danger', method: :delete do
  =fa_icon 'trash-o'
  Удалить